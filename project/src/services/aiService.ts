import type { LanguageCode, VoiceExchange } from '../types';
import { getCurrentSession } from './authService';
export interface AIMessage{role:'user'|'assistant';content:string;}
export class AIServiceError extends Error{code:'not-configured'|'request-failed'|'invalid-response';constructor(message:string,code:'not-configured'|'request-failed'|'invalid-response'){super(message);this.name='AIServiceError';this.code=code;}}
const ENDPOINT='/api/voice/chat';
async function loadDatabaseContext(){const s=getCurrentSession();if(!s?.profile?.id||s.role==='admin')return '';try{const r=await fetch(`/api/kisanlink/voice-context?role=${s.role}&userId=${encodeURIComponent(s.profile.id)}`);return r.ok?JSON.stringify(await r.json()):'';}catch{return '';}}
function localDatabaseAnswer(question:string,raw:string,language:LanguageCode){try{const d=JSON.parse(raw) as any;const q=question.toLowerCase();const farmer=d.farmer;const buyer=d.buyer;const listings=Array.isArray(d.listings)?d.listings:[];const buyers=Array.isArray(d.buyers)?d.buyers:[];const requests=Array.isArray(d.requests)?d.requests:[];const crop=farmer?.id?(listings[0]?.crop_name||listings[0]?.cropName||''):'';const qty=farmer?.id?(listings[0]?.quantity_quintals||listings[0]?.quantityQuintals||0):0;const buyerCrops=buyer?.crops_json?JSON.parse(buyer.crops_json):buyer?.crops||[];const cropWords=['crop','produce','selling','sell','what am i selling','పంట','పంటను','फसल','पीक'];const buyerWords=['buyer','buyers','who can buy','खरीदार','खरेदीदार','కొనుగోలు','కొనేవారు'];const requestWords=['request','requests','connect','connection','अभ्यर्थన','अनुरोध','विनंती','కనెక్షన్','అభ్యర్థన'];const farmerWords=['farmer','farmers','రైతు','किसान','शेतकरी'];const demandWords=['want','need','demand','wanted','కావాలి','చాహिए','चाहिए','हवे'];const has=(words:string[])=>words.some(w=>q.includes(w));if(has(cropWords)){if(crop)return `Your saved crop is ${crop}${qty?` with ${qty} quintals`:''}. This is from your live KisanLink database.`;return 'No crop has been saved yet. Add your crop details on the home page first.';}if(has(demandWords)&&buyer){return `Your current buyer demand is ${buyerCrops.join(', ')||'not set'}, with a typical quantity of ${buyer.typical_quantity||0} quintals. You can update it in My demand.`;}if(has(buyerWords)&&farmer){if(!buyers.length)return 'There are no registered buyers in the database.';return `I found ${buyers.length} registered buyers: ${buyers.slice(0,4).map((b:any)=>b.name).join(', ')}. The highest trust scores are stored in the database.`;}if(has(farmerWords)&&buyer){const wanted=buyerCrops.map((x:string)=>x.toLowerCase());const matches=listings.filter((l:any)=>wanted.some((w:string)=>String(l.crop_name||l.cropName).toLowerCase().includes(w)));if(!matches.length)return 'No current farmer listing matches your buyer demand.';return `I found ${matches.length} farmer listings matching your demand. The latest is ${matches[0].crop_name||matches[0].cropName} from ${matches[0].farmer_name||matches[0].farmerName}, ${matches[0].quantity_quintals||matches[0].quantityQuintals} quintals.`;}if(has(requestWords)){if(!requests.length)return 'You have no connection requests yet.';return requests.slice(0,5).map((r:any)=>`${r.buyerName||r.farmerName}: ${r.status} for ${r.cropName}, ${r.requestedQuantity} quintals.`).join(' ');}if(q.includes('contact')||q.includes('phone')||q.includes('mobile')||q.includes('email')||q.includes('नंबर')||q.includes('ఫోన్')){const accepted=requests.filter((r:any)=>r.status==='accepted');if(!accepted.length)return 'No accepted connection is available, so protected contact details cannot be shared yet.';return accepted.map((r:any)=>r.buyerName?`${r.buyerName}: ${r.buyerMobile||'mobile unavailable'}${r.buyerEmail?`, ${r.buyerEmail}`:''}`:`${r.farmerName}: ${r.farmerMobile||'mobile unavailable'}${r.farmerEmail?`, ${r.farmerEmail}`:''}`).join(' ');}if(language==='hi')return 'मैंने आपका लाइव KisanLink डेटाबेस देखा है। अपनी फसल, खरीदार, मांग या कनेक्शन के बारे में पूछें।';if(language==='te')return 'నేను మీ లైవ్ KisanLink డేటాబేస్‌ను చూశాను. మీ పంట, కొనుగోలుదారులు, డిమాండ్ లేదా కనెక్షన్ల గురించి అడగండి.';if(language==='mr')return 'मी तुमचा लाइव्ह KisanLink डेटाबेस तपासला आहे. तुमच्या पिकाबद्दल, खरेदीदारांबद्दल, मागणीबद्दल किंवा कनेक्शनबद्दल विचारा.';return 'I checked your live KisanLink database. Ask me about your crop, buyers, demand, farmer listings, or connection requests.';}catch{return 'I could not read the current KisanLink database context. Please try again.';}}
export async function askAgriculturalAI(messages:AIMessage[],language:LanguageCode):Promise<string>{const context=messages.slice(-12);const db=await loadDatabaseContext();const last=context.filter(m=>m.role==='user').at(-1)?.content||'';const dbMsg=db?{role:'user' as const,content:`KISANLINK DATABASE CONTEXT — trusted live application data for this logged-in user. Use it as the source of truth. Do not invent buyers, farmers, crops, requests, prices, or contacts. Only reveal another party's mobile/email when the request status is accepted.\n${db}`} : null;const aiMessages=dbMsg?[dbMsg,...context]:context;try{const r=await fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({language,messages:aiMessages,systemPrompt:'You are KisanLink KisanVoice. Understand Indian languages, dialects, transliteration and mixed-language farmer speech. Answer directly and practically in the selected language. Use KISANLINK DATABASE CONTEXT as the source of truth. Never invent live application records or protected contact details.'})});if(!r.ok){if(db)return localDatabaseAnswer(last,db,language);throw new AIServiceError('KisanVoice AI is unavailable.','request-failed');}const p=await r.json() as any;const content=typeof p.response==='string'?p.response:typeof p.message?.content==='string'?p.message.content:Array.isArray(p.choices)?p.choices[0]?.message?.content||p.choices[0]?.text:undefined;if(typeof content!=='string'||!content.trim())return db?localDatabaseAnswer(last,db,language):'I could not generate an answer. Please try again.';return content.trim();}catch(e){if(e instanceof AIServiceError&&e.code==='request-failed'&&db)return localDatabaseAnswer(last,db,language);if(db)return localDatabaseAnswer(last,db,language);throw e instanceof AIServiceError?e:new AIServiceError('KisanVoice is temporarily unavailable.','request-failed');}}
export function exchangesToMessages(exchanges:VoiceExchange[]):AIMessage[]{return exchanges.slice(-12).map(e=>({role:e.role,content:e.text}));}
