import { Sprout, Bell, ChevronRight, Mic, ShieldCheck, Calculator, LogOut, Keyboard, Send } from 'lucide-react';
import { useApp } from '../../AppContext';
import { Card } from '../ui/Card';
import { Button } from '../ui/Button';
import { Chip } from '../ui/Chip';
import { CropQualityScreen } from './CropQualityScreen';
import { useMemo, useState } from 'react';

export function HomeScreen(){
 const{t,setActiveTab,setVoiceInputMode,buyers,setView,view,setAuthenticated,profile,setProduce}=useApp();
 const[showCropQuality,setShowCropQuality]=useState(false); const[cropName,setCropName]=useState(''); const[variety,setVariety]=useState(''); const[quantity,setQuantity]=useState(''); const[grade,setGrade]=useState<'A'|'B'|'C'>('A'); const[harvestDate,setHarvestDate]=useState(''); const[expectedPrice,setExpectedPrice]=useState(''); const[formError,setFormError]=useState('');
 const matchingBuyers=useMemo(()=>{const crop=profile?.produce?.cropName.trim().toLowerCase()||'';const g=profile?.produce?.grade;if(!crop)return[];const matched=buyers.filter(b=>{const bc=(b.crop||'').toLowerCase();return bc===crop||bc.includes(crop)||crop.includes(bc);}).sort((a,b)=>Number(b.grade===g)-Number(a.grade===g)||b.trust.trustScore-a.trust.trustScore);return (matched.length?matched:buyers).slice(0,4);},[buyers,profile?.produce]);
 if(!profile)return null;
 if(!profile.produce){const save=()=>{const q=Number(quantity);const price=Number(expectedPrice)||0;if(!cropName.trim()||!harvestDate||!q||q<=0){setFormError('Enter crop name, harvest date and a valid quantity.');return;}setProduce({id:`listing-${profile.id}`,farmerId:profile.id,farmerName:profile.name,district:profile.district,state:'Telangana',cropName:cropName.trim(),variety:variety.trim()||undefined,quantityQuintals:q,grade,harvestDate,expectedPricePerQuintal:price});};return <div className="min-h-screen px-4 pt-6 sm:px-0"><div className="mx-auto max-w-2xl"><div className="mb-5 flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-brand-deep"><Sprout size={25} className="text-white"/></div><div><h1 className="text-2xl font-extrabold text-brand-deep">KisanLink</h1><p className="text-sm text-ink-soft">Tell us about your crop first. Then we find relevant buyers.</p></div></div><Card className="border-brand-mid/30"><p className="text-xs font-bold uppercase tracking-wide text-brand-deep">Step 1</p><h2 className="mt-1 text-2xl font-extrabold text-ink">Add your crop details</h2><p className="mt-1 mb-5 text-sm text-ink-soft">No buyer recommendations are shown until you submit your own crop information.</p><div className="space-y-3"><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Crop name *</span><input value={cropName} onChange={e=>{setCropName(e.target.value);setFormError('')}} placeholder="e.g. Tomato, Onion, Rice" className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none focus:border-brand-mid"/></label><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Variety</span><input value={variety} onChange={e=>setVariety(e.target.value)} placeholder="e.g. Hybrid, Basmati" className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none focus:border-brand-mid"/></label><div className="grid gap-3 sm:grid-cols-2"><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Quantity (quintals) *</span><input type="number" min="0.1" step="0.1" value={quantity} onChange={e=>setQuantity(e.target.value)} placeholder="e.g. 25" className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none"/></label><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Harvest date *</span><input type="date" value={harvestDate} onChange={e=>setHarvestDate(e.target.value)} className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none"/></label></div><div className="grid gap-3 sm:grid-cols-2"><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Crop grade</span><select value={grade} onChange={e=>setGrade(e.target.value as 'A'|'B'|'C')} className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none"><option value="A">Grade A</option><option value="B">Grade B</option><option value="C">Grade C</option></select></label><label className="block"><span className="mb-1.5 block text-xs font-bold text-ink-soft">Expected price / quintal</span><input type="number" min="0" value={expectedPrice} onChange={e=>setExpectedPrice(e.target.value)} placeholder="Optional" className="w-full rounded-2xl border border-line bg-surface-card px-4 py-3 text-sm outline-none"/></label></div>{formError&&<p className="rounded-2xl bg-red-50 px-4 py-3 text-sm font-semibold text-warning">{formError}</p>}<Button fullWidth size="lg" onClick={save}>Save crop & show buyer matches <ChevronRight size={18}/></Button></div></Card></div></div>}
 const bestBuyer=matchingBuyers[0];
 return <div className="px-4 pt-4 pb-2 sm:px-0 sm:pt-2 sm:pb-4"><div className="mb-5 flex items-center justify-between"><div className="flex items-center gap-2"><div className="flex h-10 w-10 items-center justify-center rounded-2xl bg-brand-deep"><Sprout size={22} className="text-white"/></div><div><h1 className="text-lg font-extrabold leading-none text-brand-deep">KisanLink</h1><p className="mt-0.5 text-[10px] text-ink-soft">{t('tagline')}</p></div></div><div className="flex items-center gap-2"><button onClick={()=>setView(view==='farmer'?'admin':'farmer')} className="flex h-10 w-10 items-center justify-center rounded-full border border-line bg-surface-card text-ink-soft"><ShieldCheck size={18}/></button><button onClick={()=>setActiveTab('buyers')} className="flex h-10 w-10 items-center justify-center rounded-full border border-line bg-surface-card text-ink-soft"><Bell size={18}/></button><button onClick={()=>setAuthenticated(false)} className="flex h-10 w-10 items-center justify-center rounded-full border border-line bg-surface-card text-ink-soft"><LogOut size={17}/></button><div className="flex h-10 w-10 items-center justify-center rounded-full bg-brand-soft text-sm font-bold text-brand-deep">{profile.name.split(/\s+/).map(part=>part[0]).join('').slice(0,2).toUpperCase()}</div></div></div><div className="mb-4"><h2 className="text-2xl font-extrabold text-ink">{profile.name.split(/\s+/)[0]}, your market is ready</h2><p className="mt-0.5 text-sm text-ink-soft">Suggestions use your saved crop and registered SQLite buyer records only.</p></div><Card className="mb-4 border-brand-mid/30"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wide text-ink-soft">Your crop</p><h3 className="mt-1 text-xl font-extrabold text-ink">{profile.produce.cropName}</h3><p className="mt-1 text-sm text-ink-soft">{profile.produce.quantityQuintals} quintals · Grade {profile.produce.grade}{profile.produce.variety?` · ${profile.produce.variety}`:''}</p></div><Chip tone="brand">Saved to SQLite</Chip></div><div className="mt-4 flex gap-2"><Button size="sm" variant="outline" onClick={()=>setShowCropQuality(true)}>Crop quality</Button><Button size="sm" onClick={()=>setActiveTab('buyers')}>View buyers <ChevronRight size={15}/></Button></div></Card><div className="mb-4"><h3 className="mb-2 flex items-center gap-1.5 text-sm font-bold uppercase tracking-wide text-ink-soft"><Send size={15} className="text-brand-deep"/> Buyer suggestions</h3>{bestBuyer?<Card className="border-brand-mid/30"><div className="flex items-start justify-between gap-3"><div><p className="text-base font-extrabold text-ink">{bestBuyer.name}</p><p className="mt-1 text-sm text-ink-soft">{bestBuyer.type} · {bestBuyer.location}</p></div><Chip tone="brand">{matchingBuyers.length===buyers.length?'Best match':'Available buyer'}</Chip></div><div className="mt-4 grid grid-cols-3 gap-2 text-xs"><div className="rounded-xl bg-surface-alt p-3"><p className="text-ink-faint">Trust</p><p className="mt-1 font-bold text-ink">{bestBuyer.trust.trustScore}/100</p></div><div className="rounded-xl bg-surface-alt p-3"><p className="text-ink-faint">Demand</p><p className="mt-1 font-bold text-ink">{bestBuyer.demandQuintals} qtl</p></div><div className="rounded-xl bg-surface-alt p-3"><p className="text-ink-faint">Grade</p><p className="mt-1 font-bold text-ink">{bestBuyer.grade}</p></div></div><Button fullWidth className="mt-4" onClick={()=>setActiveTab('buyers')}>Connect with this buyer <ChevronRight size={16}/></Button></Card>:<Card><p className="font-bold text-ink">Buyer records are loading</p><p className="mt-1 text-sm text-ink-soft">Refresh once the SQLite market records are loaded.</p></Card>}</div><Card className="mb-4 overflow-hidden border-0 bg-gradient-to-br from-brand-deep to-brand-mid"><div className="mb-3 flex items-center gap-2"><div className="flex h-8 w-8 items-center justify-center rounded-xl bg-white/15"><Mic size={18} className="text-white"/></div><span className="text-base font-bold text-white">KisanVoice</span></div><p className="mb-4 text-sm text-white/90">Ask about your crop, buyers and connection requests using live database data.</p><div className="grid grid-cols-2 gap-2"><button onClick={()=>{setVoiceInputMode('voice');setActiveTab('voice')}} className="flex flex-col items-center justify-center gap-2 rounded-2xl bg-white/15 py-3.5"><Mic size={22} className="text-white"/><span className="text-sm font-semibold text-white">{t('tapToSpeak')}</span></button><button onClick={()=>{setVoiceInputMode('type');setActiveTab('voice')}} className="flex flex-col items-center justify-center gap-2 rounded-2xl bg-white/10 py-3.5"><Keyboard size={22} className="text-white"/><span className="text-sm font-semibold text-white">{t('tapToType')}</span></button></div></Card><div className="grid grid-cols-2 gap-3"><button onClick={()=>setActiveTab('advisor')} className="flex flex-col items-center gap-2 rounded-3xl border border-line bg-surface-card p-4"><Calculator size={22} className="text-brand-deep"/><span className="text-sm font-semibold text-ink">{t('netReturnAdvisor')}</span></button><button onClick={()=>setActiveTab('buyers')} className="flex flex-col items-center gap-2 rounded-3xl border border-line bg-surface-card p-4"><Send size={22} className="text-market-deep"/><span className="text-sm font-semibold text-ink">Buyer connections</span></button></div>{showCropQuality&&<CropQualityScreen onClose={()=>setShowCropQuality(false)}/>}</div>;
}
